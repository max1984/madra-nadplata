import { createTheme } from '@mantine/core';

export const theme = createTheme({
  fontFamily: '"DM Sans", system-ui, sans-serif',
  headings: { fontFamily: '"Inter", system-ui, sans-serif', fontWeight: '800' },
  primaryColor: 'blue',
  defaultRadius: 'md',
});
